// Generated by scripts/convert-gooey-swift.mjs.
// Source of truth: packages/gooey/src/styles/design-tokens.css and packages/gooey/src/components/ui.
// Re-run the converter instead of editing generated files directly.
import SwiftUI
import GooeyFoundation

public enum GooeyInputKind: Sendable {
  case text
  case email
  case password
}

public struct GooeyInput: View {
  @Binding public var text: String
  public var label: String
  public var placeholder: String
  public var kind: GooeyInputKind
  public var isDisabled: Bool

  @Environment(\.gooeyTheme) private var theme

  public init(
    text: Binding<String>,
    label: String,
    placeholder: String = "",
    kind: GooeyInputKind = .text,
    isDisabled: Bool = false
  ) {
    self._text = text
    self.label = label
    self.placeholder = placeholder
    self.kind = kind
    self.isDisabled = isDisabled
  }

  public var body: some View {
    Group {
      if kind == .password {
        SecureField(placeholder, text: $text)
      } else {
        TextField(placeholder, text: $text)
      }
    }
    .font(.system(size: 14, weight: .regular, design: .rounded))
    .frame(height: 40)
    .padding(.horizontal, 12)
    .foregroundStyle(theme.color("foreground"))
    .background(theme.color("background"))
    .clipShape(RoundedRectangle(cornerRadius: 6, style: .continuous))
    .overlay {
      RoundedRectangle(cornerRadius: 6, style: .continuous)
        .stroke(theme.color("input"), lineWidth: 1)
    }
    .disabled(isDisabled)
    .opacity(isDisabled ? 0.5 : 1)
    .accessibilityLabel(Text(label))
  }
}
